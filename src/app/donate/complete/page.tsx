'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
import { useSearchParams } from 'next/navigation'
export default function Complete(){const params=useSearchParams();const gateway=params.get('gateway');const reference=params.get('reference')||'';const transactionId=params.get('transaction_id')||params.get('transactionId')||'';const [state,setState]=useState<'loading'|'success'|'failed'>('loading');const [error,setError]=useState('');const [token,setToken]=useState('');useEffect(()=>{if(!reference)return setState('failed');(async()=>{try{const endpoint=gateway==='flutterwave'?'/api/payments/flutterwave/verify':'/api/payments/paystack/verify';const body=gateway==='flutterwave'?{reference,transactionId}:{reference};const res=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await res.json();if(!res.ok||!data.success)throw new Error(data.error||'Payment could not be verified');setToken(data.receiptToken||'');setState('success')}catch(e){setError(e instanceof Error?e.message:'Payment verification failed');setState('failed')}})()},[gateway,reference,transactionId]);return <main className="section"><div className="container" style={{maxWidth:720,textAlign:'center'}}>{state==='loading'?<><h1 className="serif">Confirming your donation…</h1><p className="muted">Please wait while we securely verify your payment.</p></>:state==='success'?<><div className="eyebrow">Thank you</div><h1 className="serif">Your donation was successful.</h1><p className="muted">Reference: <strong>{reference}</strong></p><div style={{display:'flex',gap:12,justifyContent:'center',marginTop:28,flexWrap:'wrap'}}><a className="btn btn-primary" href={`/api/receipts/${encodeURIComponent(reference)}${token?`?token=${encodeURIComponent(token)}`:''}`}>View / Print Receipt</a><Link className="btn btn-light" href="/">Return Home</Link></div></>:<><h1 className="serif">We could not confirm the payment.</h1><p className="muted">{error}</p><Link className="btn btn-primary" href="/donate">Try Again</Link></>}</div></main>}
