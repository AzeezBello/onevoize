import CrudPage from '@/components/admin/CrudPage'
import { requireAdmin } from '@/lib/admin'
export default async function Page(){const {supabase}=await requireAdmin();const {data}=await supabase.from('blog_posts').select('*').order('created_at',{ascending:false});return <CrudPage title="Blog" description="Publish news, impact stories, updates and insights." table="blog_posts" displayKey="title" subtitleKey="excerpt" fields={[{key:'title',label:'Post title',required:true},{key:'excerpt',label:'Excerpt',type:'textarea'},{key:'content',label:'Content',type:'textarea',required:true},{key:'category',label:'Category'},{key:'author_name',label:'Author'},{key:'featured_image',label:'Featured image URL',type:'url'},{key:'status',label:'Status'},{key:'published_at',label:'Published at',type:'date'}]} initialRows={data||[]}/>} 
