import { requireAdmin } from '@/lib/admin'
export default async function Volunteers(){const {supabase}=await requireAdmin();const {data}=await supabase.from('volunteers').select('*').order('created_at',{ascending:false});return <div className="p-6 md:p-8"><p className="text-xs uppercase tracking-[.16em] text-[#174b35] font-bold">People</p><h1 className="text-3xl font-serif mt-1">Volunteers</h1><p className="text-gray-500 mt-2 mb-7">Review people who want to support the project.</p><div className="bg-white border rounded-2xl overflow-auto"><table className="w-full text-sm"><thead className="bg-gray-50"><tr className="text-left"><th className="p-4">Name</th><th className="p-4">Email</th><th className="p-4">Phone</th><th className="p-4">Interest</th><th className="p-4">Date</th></tr></thead><tbody className="divide-y">{(data||[]).map((v:any)=><tr key={v.id}><td className="p-4 font-medium">{v.name}</td><td className="p-4">{v.email}</td><td className="p-4">{v.phone||'—'}</td><td className="p-4">{v.interest||'—'}</td><td className="p-4 text-gray-500">{new Date(v.created_at).toLocaleDateString()}</td></tr>)}</tbody></table>{!data?.length&&<div className="p-12 text-center text-gray-500">No volunteer applications yet.</div>}</div></div>}
