import CrudPage from '@/components/admin/CrudPage'
import { requireAdmin } from '@/lib/admin'
export default async function Page(){const {supabase}=await requireAdmin();const {data}=await supabase.from('campaigns').select('*').order('created_at',{ascending:false});return <CrudPage title="Campaigns" description="Create fundraising campaigns and track their targets." table="campaigns" displayKey="title" subtitleKey="description" fields={[{key:'title',label:'Campaign title',required:true},{key:'description',label:'Description',type:'textarea',required:true},{key:'target_amount',label:'Target amount',type:'number',required:true},{key:'image_url',label:'Image URL',type:'url'},{key:'start_date',label:'Start date',type:'date'},{key:'end_date',label:'End date',type:'date'},{key:'status',label:'Status'}]} initialRows={data||[]}/>} 
