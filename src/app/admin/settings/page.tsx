import { requireAdmin } from '@/lib/admin'
export default async function Settings(){const {user,profile}=await requireAdmin();return <div className="p-6 md:p-8 max-w-4xl"><p className="text-xs uppercase tracking-[.16em] text-[#174b35] font-bold">System</p><h1 className="text-3xl font-serif mt-1">Settings</h1><div className="grid md:grid-cols-2 gap-5 mt-7"><section className="bg-white border rounded-2xl p-6"><h2 className="font-semibold">Account</h2><p className="text-sm text-gray-500 mt-2">Signed in as</p><p className="mt-1">{user?.email}</p><p className="text-sm text-gray-500 mt-4">Role</p><p className="mt-1 capitalize">{profile?.role?.replace('_',' ')||'Admin'}</p></section><section className="bg-white border rounded-2xl p-6"><h2 className="font-semibold">Supabase</h2><p className="text-sm text-gray-500 mt-2">Authentication and content storage are powered by Supabase. Add your project keys to <code>.env.local</code> before running locally.</p></section></div></div>}
